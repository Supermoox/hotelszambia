<%= form_with(model: wallpaper, local: true) do |form| %>
  <% if wallpaper.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(wallpaper.errors.count, "error") %> prohibited this wallpaper from being saved:</h2>

      <ul>
      <% wallpaper.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :wallpaper_name %>
  </div>

  <div class="field">
    <%= form.label :accommodation_id %>
    <%= form.text_field :accommodation_id, id: :wallpaper_accommodation_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
