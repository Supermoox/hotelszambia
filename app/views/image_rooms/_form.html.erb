<%= form_with model: image_room, local: true, html: { multipart: true }  do |form| %>
  <% if image_room.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(image_room.errors.count, "error") %> prohibited this image_room from being saved:</h2>

      <ul>
      <% image_room.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :image_room_name %>
  </div>

  <div class="field">
    <%= form.label :room_id %>
    <%= form.text_field :room_id, id: :image_room_room_id %>
  </div>


  <div class="field">
    <%= form.label :image %>
    <%= form.file_field :image %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
